# Scripts legacy (no ejecutar)

Migraciones one-off usadas al partir CSS/HTML o extraer la escena Spline.

No forman parte del flujo diario. Ejecutarlos puede sobrescribir `css/` o `index.html` con rangos desactualizados.

- `split_css.py`
- `patch_index.py`
- `extract_spline_scene.py`
